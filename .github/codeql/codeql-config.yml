# Code scanning: limit noise from dependencies and build outputs.
# https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/customizing-your-advanced-setup-for-code-scanning
#
# Triage open alerts by rule (local): scripts/export-code-scanning-alerts.ps1 (GitHub CLI + gh auth login).
#
# In-repo MaD pack (.github/codeql/deltai-python-models) is for local CodeQL workspace use only; listing it
# under packs: would pull from GHCR in Code Scanning. Publish the pack if you need it in CI.
paths-ignore:
  - "**/node_modules/**"
  - "**/.venv/**"
  - "**/venv/**"
  - "**/dist/**"
  - "**/build/**"
  - "**/__pycache__/**"
  - "**/.eggs/**"
  - "**/*.egg-info/**"
