name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint-and-test:
    uses: ./.github/workflows/python-verify.yml
    with:
      pytest_args: '-q -m "not slow"'
      collect_junit: false
      run_ruff_format_check: false
