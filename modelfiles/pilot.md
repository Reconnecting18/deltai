# Pilot / focus sessions (reference)

When the router detects a GPU focus or pilot workload, queries may be classified for session-aware routing. Ingest and tools still follow core protocols: ground answers in tool output and operator-provided context, not assumptions.
